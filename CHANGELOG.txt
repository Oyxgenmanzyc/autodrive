2026-09-18 | 3.1.05_5 | Independent Gate and paired refinement
# 2026-09-18 运行修复：移除每5轮难负例更新中的跨卡 all_gather_object；原正式probe在epoch 4后触发NCCL watchdog并以134退出。改为各rank本地难例池，标签/模型/损失不变，兼容故障前checkpoint续训。服务器复测待完成。
# 3.1.05_5：独立修改 Gate 与配对专精训练

修改日期：2026-09-18。涉及文件：`gated_refinement/{labels,data,model,training}.py`、`run_gated_refinement.py`、`run_3_1_05_5.sh`、`install_3_1_05_5.sh`、`test_gated_refinement.py`、服务器文档和 CHANGELOG。预期影响是检验可修正场景的可辨识性并减少identity主导的决策失效；论文中应作为独立Gate/动作头与配对训练的新增实验，不能把Oracle上界写成学习模型提升。

## 实验动机与证据

3.1.05_4 离线受约束 Oracle：训练 85109 场景中 1378 个可改善（1.62%），验证 18179 场景中 1557 个可改善（8.57%）。验证 Oracle 为 93.1259 PDM，原输出 87.5316；这是利用真实评分选择动作的上界，不是模型效果。epoch00 扩展阈值诊断在 risk_limit=1、margin=0 时 edits=0，说明仅放宽风险阈值无法解除未触发修改的问题。gain Pearson 0.241、risk Pearson 0.288 不能证明可部署的尾部识别能力。

## 架构和标签

- 冻结原 K67、PCS、TRV。新增模块接在 TRV 最终轨迹之后。
- Gate 读取原轨迹的几何特征、采样 BEV、agent/ego context。收益与风险标签不进入网络输入。
- 复用 Cross-Attention，为修改动作预测 gain/risk；Gate 单独输出是否值得修改，另设分项改善原因与 regret 辅助头。
- 不再让所有修改动作与固定为零的 identity gain 竞争。Gate 开启后在通过风险过滤的非 identity 动作中按预测收益排序；所有动作被过滤时输出 identity。
- 现有动作空间只有额外制动：4 个 onset × 3 个 additional deceleration × 2 个 ramp = 24 动作，另加 identity。无法加速、改变横向路径、直接恢复过度制动。此版不扩充动作空间，从而能复用原评分。
- 所有正例动作必须 NC/DAC/TTC/comfort/direction 不退化、PDM 增益 > 1e-6；同时满足增益 >= 0.02，或 NC/DAC/TTC 任一改善。0.02 是预先设定的实验超参数，不是已验证的最优值。
- 监督 need_modify、best_action、onset/deceleration/ramp、oracle_gain，以及 NC/DAC/TTC/comfort/progress 改善标签。它们说明“哪些指标改善”，不能证明事故根因是刹车过晚。
- 原轨迹存在安全失败但动作库无有效解时标记 unresolved_by_bank：对于本编辑器的 Gate 是负例，不代表真实场景安全。
- 正例动作采用有效动作上的 soft target。只有正例参与动作排序/收益回归；Gate/原因/regret 在配对全体样本上训练，联合阶段风险头对所有样本训练。

## 相似负例与训练步骤

1. 只用训练集 selected trajectory 的位置、速度、加速度、航向正余弦做标准化描述子；每个正例找 4 个不同 recording log 的最近负例。标准化统计只从训练集拟合。该距离是轨迹运动学相似代理，尚未覆盖完整的交互语义。
2. 每组 1 个正例、2 个配对负例、1 个随机负例。Gate 使用 BCE 加配对排序损失，不叠加巨大 pos_weight。随机负例和配对负例每轮重新抽选。
3. 一轮覆盖所有正例一次（仅 DDP 尾部补齐）；不按 85109 定义训练轮。此版 20 轮比旧版全数据 20 轮更新次数少，日志会保存正例数量。验证仍覆盖完整 18179 场景。
4. probe 阶段只训练小模块的 Gate、原因、regret；动作头冻结。主感知/生成器均使用原冻结缓存。它是小型上下文探针，不等同于仅训练线性分类器。
5. joint 阶段从最佳 probe 初始化，训练 Gate 和动作/风险分支。新增 branch 不直接加载旧失败 refiner 的收益头。
6. 每5轮在已访问的训练负例中收集Gate最高误报分数，更新最多1024个难负例池。后续第4个负例槽以50%概率从该池抽取，另外50%仍随机。配对正例/两个相似负例不变；不会使用val误报挖掘训练样本，也不会额外扫描205GB缓存。所有正例仍每轮至少覆盖一次。

## 校准与验证

- 官方 navtrain val 按完整 log 确定性分成 calibration/audit 两半；log 不跨分区。训练样本不从两部分检索或采样。
- probe 按 calibration AUPRC 选 checkpoint；joint 按 calibration PDM 选 checkpoint，阈值也仅用 calibration。
- audit 原始分布结果仅用于诊断；若据此继续修改模型，它就不再是完全未使用的最终测试集。navtest 不用于训练/选阈值。
- 输出 AUPRC、正例比例、Recall/Precision at 5%/10% budget（阈值并列时包括所有并列样本并报告实际触发率）。过采样 Gate sigmoid 不是已校准真实概率；阈值在自然分布上选择。
- 每轮保存原始验证预测、所有阈值组合、calibration/audit 报告。风险约束检查分项均值，属于经验约束，不构成单场景安全保证。
- 最终必须看 audit/navtest PDM 净增益、有益/有害修改、TTC 救回/新增失败，不能只看 Gate 召回率。

## 缓存兼容

复用 PCS 205GB、完整 edit cache 98MB；prepare 只生成 gate labels/pairs/CSV，不读取原图、LiDAR、GTRS 或重新算 PDM。正式训练需要 PCS+edit+gate cache。在线 navtest 评估仍需原 checkpoint、backbone、anchor、PCS/TRV checkpoint、navtest 数据和 metric cache。

评分缓存与新模型代码分开版本校验：严格检查动作 geometry hash、缓存 schema、冻结 generator provenance、candidate manifest、records、逐 block 内容 hash；加载时逐场景验证 selected 与原 proposals[mode] 精确相等。不会因为模型训练代码变化而要求重新评分。旧 sources 元数据完整保留；在线验证继续检查原 selector checkpoint SHA。新的 gate cache 与新增源文件 SHA 绑定。

## 验证状态

遵循本项目 skill：not run locally; pending server smoke test。新增服务器单元测试覆盖标签保护、噪声增益、软目标、匹配防泄漏、DDP 配对采样、Gate/identity 解耦、并列 AUPRC、校准和梯度；训练效果待服务器验证。未声称已获得 PDM 提升。


3.1.05_3 Timing Query Cross-Attention???/????
????
- 2026-09-11
????
- ???? timing query ??? query ???????? Q ?????? K/V?????????
????
- ??BridgeAD??????MTR???????ControlNet?????????????????
- 8?????Q?????????/???????????Cross-Attention????????
- ?????????K67?PCS/TRV?????????????LiDAR????GT?????
- ??/??/???/???????????FP32??????????????RMS??0.1?
- ?????????????/??????????jerk?DDP??????????
- ???205GB PCS????GT sidecar???????????risk-input???metric cache???
- ????checkpoint??????????/DDP smoke?4?BS32????????PDM???
????
- navsim/agents/diffusiondrive/timing_attention/
- navsim/planning/script/run_timing_attention.py
- scripts/pcs/run_3_1_05_3_attention.sh
- tests/test_timing_attention.py
- docs/experiment_3_1_05_3_timing_attention.md
- docs/server_3_1_05_3_timing_attention.md
????
- ????????????navhigh?????check-init?smoke?????PDM???
- ??loss-only????PDM 89.181861?89.090669???????????????????Cross-Attention??????
???????
- timing query????????????????????????planning query????
- 0.1????????????????????LiDAR?????????????????

修改主题：3.1.05_3：生成器刹车时机监督，固定已验证的 PCS＋TRV
修改日期：2026-09-10

2026-09-11 数据依赖修正：服务器确认原94GB K67 training cache已按先前清理建议删除。
训练输入改为现有 PCS cache 保存的冻结 K67 场景上下文，因此无需重建94GB缓存；
原始错误依赖已移除，服务器须使用本次更新后的压缩包。

2026-09-11 换行来源修正：将旧PCS/TRV来源校验涉及的8个文件统一记录为LF，
避免Windows打包产生CRLF后与服务器checkpoint中保存的源码SHA256不一致；无逻辑变化。

2026-09-11 smoke修正：冻结父生成器处于eval时，显式调用轨迹头的训练/推理入口，
避免训练步骤误走forward_test并缺少trajectory_loss；新增分支调度回归测试。

修改目的：
纠正 timing-weighted-gain-selector 分支把 3.22 用在筛选端的方向偏差。
本版以原 TRV 611e87b 为基础，将 3.22 的时机监督用于轨迹生成，
检验候选质量改善是否能在固定筛选器下改善 TTC 和总 PDM。

核心改动：
- 迁移原 change/20260713-brake-timing-supervision-3.22 的 GT 软刹车时机损失。
- 仅微调轨迹头，冻结感知/上下文、anchor、原分类头参数；PCS/TRV 不参加训练。
- 监督 GT anchor 匹配模式的刹车软轮廓和累计启动轮廓，保留原来的风险场景激活条件。
- control/timing 两组使用同一初始生成器、数据、种子、优化器、训练步数，权重分别为 0/0.1。
- 独立小型 GT targets sidecar；复用现有 205GB PCS cache 中冻结的 K67 BEV/agent/ego 场景特征。
- 不依赖此前已删除的 94GB training cache；不重复计算感知特征，也不读取 PCS 的 PDM 标签训练生成器。
- 保留原模型和全部 PCS/TRV 实现文件，新增训练封装通过 decoder 输出 hook 连接梯度。
- 对新旧生成器同场景同种子生成全部 K67 候选，固定同一 PCS/TRV 评估并保存全部候选标签。
- 严格检查原生成器、anchor、PCS、TRV 的 checkpoint 来源，明确记录新生成器应用到旧筛选器的实验。
- 对 DDP 中稀有激活场景按全局 active count 归一化；不按每张卡各自均值等权放大。

涉及文件：
navsim/agents/diffusiondrive/generator_timing/
navsim/planning/script/run_generator_timing.py
navsim/planning/script/compare_generator_timing.py
scripts/pcs/run_3_1_05_3_generator.sh
tests/test_generator_timing.py
docs/experiment_3_1_05_3_generator_timing.md
docs/server_3_1_05_3_generator_timing.md
README.md, .gitattributes

预期影响与论文边界：
把“生成时机监督是否有效”和“筛选器是否善于利用改善后的候选”分开检验。
原 3.22 不保证所有模式都变好；通过全部候选 TTC 合格率、PDM 均值、oracle 和最终 PDM 验证。
需与等时长 control 比较，不能只与未经额外训练的旧 checkpoint 比较后宣称 3.22 有效。

测试/验证结果：
未在本地运行测试、语法检查或 smoke；按项目工作流等待服务器 navhigh 测试。
已补充梯度、GT 匹配、无激活、对照关闭、静止数值稳定、冻结范围等服务器测试。
89.181861 为上一版 TRV 已有结果，本版尚无训练及 PDM 结果。

------------------------------------------------------------
修改主题：3.1.05_2：原 PCS hard negatives 的三因子风险否决器（TRV）
修改日期：2026-09-09

修改目的：
保留原 PCS 89.0756 的候选排序收益，仅对原 PCS 实际选择的 candidate 与 base
训练 NC、DAC、TTC 相对风险，减少高置信新增零分；无正收益则作废本实验。

核心改动：
- 冻结原 PCS epoch5 作为 proposer，不重新学习候选排序。
- 从现有 K67 cache 生成可续存的小型 PCS/base 决策对 cache，不重跑生成器或 PDM。
- 相对标签定义为 clamp(base 指标 - candidate 指标, 0, 1)，保留 NC 软退化。
- 三个独立风险头使用 candidate/base/residual 特征及 PCS/原 selector 预测信息。
- focal BCE 对相对退化、新增零分和 PCS 输给 base 的 hard negatives 提高权重。
- 每个 epoch 在独立 val 上搜索三项 OR 否决阈值；搜索包含完全不否决的原 PCS。
- 完整验证同次输出 TRV、原 PCS 和 base，直接统计否决收益及新增零分。

涉及文件：
navsim/agents/diffusiondrive/pcs/veto.py
navsim/agents/diffusiondrive/pcs/veto_data.py
navsim/agents/diffusiondrive/pcs/veto_training.py
navsim/planning/script/run_pcs_veto.py
scripts/pcs/run_3_1_05_2_veto.sh
tests/test_pcs_veto.py
docs/experiment_3_1_05_2_triple_risk_veto.md
docs/server_3_1_05_2_triple_risk_veto.md
README.md

验证状态：
新增相对风险标签、OR 否决、阈值校准、梯度隔离和损失反传测试。
navhigh 初始 13 项测试、pair pilot、完整 pair cache、四卡训练、eval smoke 与
12,146 场景完整 navtest 均已完成。

正式结果：
- checkpoint：epoch=02.ckpt；val 校准阈值为 NC/DAC/TTC 均 0.5。
- TRV 89.181861，原 PCS 89.075571，同次 base 88.488936。
- 相对 PCS +0.106290 点，相对 base +0.692925 点。
- 11,857 次 PCS 换选中只否决 448 次，保留 11,409 次。
- 新增零分由 231 降到 199，同时救回零分由 193 降到 177。
- 结果满足预先规定的 PDM 正增益和新增零分下降条件，保留本实验。

服务器修正（2026-09-09）：
- 完整训练在 epoch 0 的 540/665 处发现极少量 PCS argmax 随 batch 形状翻转。
- 训练改为以预生成决策对的 PCS mode 为准，只对 base mode 保留严格一致性校验。
- 新增 train/val PCS mode mismatch rate，区分数值近似翻转与缓存身份错误。

------------------------------------------------------------

修改主题：新 3.1.05：冻结 K67 生成器的 PDM Candidate Scoring（PCS）
修改日期：2026-09-07

修改目的：
结束 SPR 重构路线；以原 3.1.02 的 K67 checkpoint 为固定生成器，单独检验 PDM 监督能否改善候选选择。
新分支 change/20260907-pdm-candidate-scoring-3.1.05，不覆盖旧 SPR 分支。

涉及文件：
navsim/agents/diffusiondrive/pcs/（V2 评分组件、MIT 许可、标签评分、缓存与训练）
navsim/agents/diffusiondrive/transfuser_model_v2.py
navsim/agents/diffusiondrive/modules/scheduler.py
navsim/planning/script/run_pcs.py
scripts/pcs/run_3_1_05.sh
tests/test_pcs.py
docs/experiment_3_1_05_pcs.md
docs/server_3_1_05_pcs.md
README.md

核心改动：
- 从 DiffusionDriveV2 1cd12a1 迁移单层轨迹条件 BEV/车辆/自车交互与五项 PDM 评分头，保留许可及来源。
- 采用缓存的最终推理候选训练；冻结生成器参数与运行模式，保留原 67 条轨迹、原分类头及两步去噪。
- 保留 NC=0.5 的原始标签；不引入 EP-only 排序、RL、词表、候选扩增、重构、精筛或旧多重门控。
- 借鉴 3.20.10 的梯度隔离和同次推理对照设计，未迁入风险代理标签和 shield。
- 采用 V2 的逐参考 progress 归一化；每个 cache shard 首个场景自动与官方逐条评分核对。
- 新增候选上限诊断、四卡分片缓存、仅评分器训练、同候选 PCS/原 selector 配对验证。
- 缓存/权重严格检查基础 checkpoint、anchor、seed 与实现哈希；训练拒绝 navtest 标签及未完成 cache。
- 每 token 固定扩散噪声；训练验证按场景索引去除 DDP padding 后汇总。
- 同时修复旧 WarmupCosLR 的 verbose 位置参数兼容问题。

预期影响：
直接监督选择器判断候选的 PDM 子指标；是否达到 88.6 必须经过候选诊断、独立验证和正式实测。
新增 navtrain metric cache（若已有则复用）与候选特征/标签 cache；原训练特征及 navtest metric cache 可复用。

测试/验证结果：
新增 K67、梯度隔离、候选重排、权重往返、PDM 归一化边界、数据划分与缓存冲突测试。
not run locally; pending server smoke test。依项目工作流，本地不运行单元测试、语法检查、smoke 或完整训练。
未获得本版本新的 PDM 指标；服务器须依文档完成 test/pilot/smoke/eval-smoke。

与论文撰写的关系：
评分网络源自 DiffusionDriveV2，需引用上游。PCS 是实验命名，不把改名或常规移植描述为原创模块。
通过固定生成器和同次配对验证，隔离候选选择收益与生成质量变化。

------------------------------------------------------------

修改主题：新 3.1.02：保留原算法，迁回 3.1.02_2 的只读诊断能力
修改日期：2026-09-05

修改目的：
以 0c18ceb（原版 DiffusionDrive + Adaptive Multimodal Anchor）为基底，按用户要求结束当前 3.1.02_2 消融排查，仅迁回不会参与预测、匹配或反向传播的统计与离线分析功能。

涉及文件：
navsim/agents/diffusiondrive/anchor_diagnostics_callback.py
navsim/agents/diffusiondrive/anchors/diagnostics.py
navsim/agents/diffusiondrive/transfuser_config.py
navsim/agents/diffusiondrive/transfuser_agent.py
scripts/anchors/diagnose_anchor_bank.py
scripts/anchors/merge_anchor_diagnostics.py
tests/test_anchor_diagnostics.py
tests/test_anchor_diagnostics_callback.py
docs/anchor_diagnostics_3_1_02.md

核心改动：
1. 从 3.1.02_2 的 utilization/diversity 诊断思想中提取只读能力，适配原版全局 ADE 静态匹配、普通 NPY bank；不直接合并其模型或损失实现。
2. 可选 anchor_diagnostics=false（默认）：启用后用 detached forward hook 记录每个 decoder 层的静态 winner / logit top1 计数、频率、活跃率、熵、分类/回归损失、非有限 batch 数和实际 optimizer group LR。
3. 统计不注册模型参数或 buffer、不写入 checkpoint、不额外调用模型 forward、不抽随机数；每个 rank 独立写 JSON，训练中不引入 collective。全局统计在训练外先求和计数再计算熵。
4. 增加原版 NPY anchor 的离线支持度、最近邻 ADE、近重复比例、覆盖率报告及总览/逐条/支持度可视化；只读原 bank，不更改生成或去重结果。
5. 原版 base + residual 构建、静态 ADE winner、focal mean + L1、全部 loss weights、DDIM 推理、optimizer/scheduler、训练缓存构造均保留。没有迁入 command mask、D_traj、动态 WTA、K-invariant focal 或 1.5% child ratio。

预期影响：
默认关闭训练诊断时保持原训练/推理数值路径；启用时仅增加观测和 I/O 开销。此版本不宣称获得新的 PDM 分数，既有约 88.44 的成绩属于原 checkpoint 与原评测配置。

测试/验证结果：
按实验工作流未在本地运行测试、语法检查或训练；待服务器执行新增单元测试和 4 卡跨两轮 smoke test。新增测试覆盖 loss/gradient/RNG/checkpoint 状态不变、分层计数、不统计验证、NaN 只记录不修复、禁止 collective、离线合并完整性、覆盖与重复度边界。

与论文撰写的关系：
本版为原 3.1.02 的观测增强版，不作为额外算法增益。3.1.02_2 的消融结果保留为独立实验，不将诊断模块归因为 PDM 改善。

------------------------------------------------------------

修改主题：3.1.02 原版 DiffusionDrive + Adaptive Multimodal Anchor
修改日期：2026-08-22

修改目的：
在原版 DiffusionDrive 上单独验证自适应多模态 anchor，不引入 3.1.01 的 temporal-pair 训练、GT-protected 梯度投影、temporal energy/ranking loss 或跨帧推理状态，保证 anchor 消融实验变量独立。

涉及文件：
navsim/agents/diffusiondrive/anchors/
navsim/agents/diffusiondrive/modules/multimodal_loss.py
navsim/agents/diffusiondrive/transfuser_agent.py
navsim/agents/diffusiondrive/transfuser_model_v2.py
scripts/anchors/
docs/adaptive_anchor_bank.md
tests/test_adaptive_anchor_bank.py
tests/test_diffusiondrive_variable_anchor.py

核心改动：
1. 从 navtrain GT 提取 `[N, 8, 2]` ego-local 轨迹，离线构建 adaptive anchor bank。
2. 通过 base split、root-conditioned tangent-normal residual codebook、support mask 和 ADE 去重生成多模态 anchor。
3. TrajectoryHead 从 anchor 文件 shape 自动推导候选数量 K，不再固定为 20。
4. decoder、分类分支、回归分支、训练 loss 和推理 gather 均沿用原版 DiffusionDrive，并支持动态 K。
5. 加载旧 checkpoint 时过滤其中的 `plan_anchor`，避免 20-anchor checkpoint 覆盖当前 adaptive bank；其余模型参数保持兼容加载。
6. 对 anchor 文件执行 shape、非空和有限值校验。
7. 未包含 temporal-pair dataset、TemporalPairAgentLightningModule、上一帧轨迹缓存、temporal noise、temporal rescoring、energy ranking 或 GT-protected gradient。

预期影响：
仅改变 DiffusionDrive 的离散轨迹候选集合及候选数量，使长尾轨迹和局部运动模式获得更好的 anchor 覆盖。计算量和显存预计近似随 K 线性增加。

测试/验证结果：
本地不运行训练或完整 smoke test；待服务器执行 anchor 单元测试、variable-K 单元测试和 4 卡 fast-dev-run。

与论文撰写的关系：
该版本作为“原版 DiffusionDrive + Adaptive Multimodal Anchor”的独立消融实验，与 3.1.01 时序优化实验分离，可用于直接比较固定 20-anchor 与 adaptive variable-K anchor 的覆盖率、训练稳定性及 NAVSIM 指标。
3.1.05_4 选后时机修正 PTR / Post-selection Timing Refiner
修改日期：2026-09-12
修改目的：固定原K67+PCS+TRV决策，只学习最终选中轨迹的制动时机与力度参数。
核心改动：
- 新增24种渐进制动变体及严格恒等动作；沿原几何路径弧长重定时，不自由改写XY。
- 单次时机query Cross-Attention，预测动作相对官方PDMS增益与安全/舒适退化风险。
- 使用全部动作标签和有害编辑监督；navtrain val校准阈值、选PDMS checkpoint，可整体回退恒等。
- 无N层折扣：层权重不等于Bellman backup，重复层数不增加独立训练样本。
- 复用原205G PCS上下文及train/test metric cache；新增可续用的128场景分块标签。
- 包含单卡/四卡Smoke、缓存分片、训练恢复与原方案对照评估脚本。
涉及文件：navsim/agents/diffusiondrive/post_selection/，run_post_selection.py，run_3_1_05_4.sh，tests/test_post_selection.py，docs/*3_1_05_4.md。
预期影响：只验证选后受控制动是否提高最终PDMS；不承诺提升。旧注意力生成器不参与。
验证结果：未在本地运行测试或Smoke，待服务器navhigh验证；新增11项行为测试。
实验记录：原方案89.1819；旧生成器注意力epoch12=89.1252、last=89.0861。
论文关系：网络图中位于PCS/TRV后、完整扩散循环之外。PTR是本实验命名，不是已有论文模块或强化学习方法。
证据限制：已有近邻候选Oracle 95.4096不等于严格同路径制动上限；本版先计算受控动作Oracle。
缓存续跑修正（2026-09-17）：重建 navtrain metric cache 时，PDM 参考轨迹可能令归一化 progress 和总分轻微漂移。PTR identity 校验改为要求动作零轨迹逐元素一致，且 NC、DAC、TTC、comfort、direction 与原缓存一致；不再错误地要求旧、新 composite PDM 总分在 1e-5 内相等。25 个动作的训练标签仍统一来自同一次新 metric cache 官方评分。
